# Core dependencies
requests>=2.31.0
psutil>=5.9.0
pillow>=10.0.0

# Optional but recommended for better security
cryptography>=41.0.0

# Optional for system tray support
pystray>=0.19.0

# Optional for icon extraction (Windows only)
pywin32>=306
